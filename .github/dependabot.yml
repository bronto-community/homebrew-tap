version: 2
updates:
  # The workflow pins actions by commit SHA, which is only safe if something
  # bumps them; otherwise the pin silently rots.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
