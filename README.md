# bronto-community Homebrew tap

[![Audit](https://github.com/bronto-community/homebrew-tap/actions/workflows/audit.yml/badge.svg)](https://github.com/bronto-community/homebrew-tap/actions/workflows/audit.yml)

Homebrew tap for the [bronto-community](https://github.com/bronto-community)
tools that support the [Bronto](https://bronto.io) observability platform.

| Cask | What it is | Upstream |
| --- | --- | --- |
| `bronto` | Community command-line client for Bronto | [bronto-cli](https://github.com/bronto-community/bronto-cli) |
| `compy` | Local OpenTelemetry Collector manager for the dev loop | [compy](https://github.com/bronto-community/compy) |

## Install

```sh
brew trust --tap bronto-community/tap
brew install bronto-community/tap/bronto
brew install bronto-community/tap/compy
```

Recent Homebrew requires explicit trust for third-party taps and silently
ignores casks from untrusted ones. If you would rather not trust the whole tap
(which covers all current *and future* casks in it), trust them individually:

```sh
brew trust --cask bronto-community/tap/bronto bronto-community/tap/compy
```

`bronto` installs shell completions for bash, zsh, and fish automatically.

`compy` puts itself in your menu bar on install. To remove it, use "Remove from
Menu Bar" in its menu or run `compy tray uninstall`.

## Updating

```sh
brew update && brew upgrade bronto compy
```

## Reporting problems

Report anything about the tools themselves on their own issue trackers —
[bronto-cli](https://github.com/bronto-community/bronto-cli/issues) or
[compy](https://github.com/bronto-community/compy/issues). Open an issue
**here** only for problems with the tap itself, such as a cask that fails
`brew install` for a packaging reason.

## How this repo is maintained

Both casks are generated by [goreleaser](https://goreleaser.com/) from their
project's release pipeline and pushed here by a bot. **Manual edits to
`Casks/*.rb` are overwritten by the next release** — fix the cask template in
the upstream repo's `.goreleaser.yaml` instead.

CI audits every cask in `Casks/` on push, on pull request, and weekly (release
assets can rot between releases). Because goreleaser's built-in cask template
is not `brew style`-clean and exposes no knob to change that, the workflow
normalizes style on `main` and commits the result rather than failing on every
release.

## Verifying artifacts

Every bronto-cli release ships a cosign-signed `checksums.txt` and
per-archive SBOMs — see the
[security policy](https://github.com/bronto-community/bronto-cli/blob/main/SECURITY.md)
for the verification one-liner.

Note that the published binaries are **not** Developer ID notarized, so both
casks strip the macOS quarantine attribute on install to avoid Gatekeeper's
"cannot verify" dialog.
